app.factory('eventFactory', function($http, AuthService) {
    //get all events for a single user

    //get all events for a single classroom

    //set events from a classroom this include broadcasting it to all other users involved

    //set events from a user


})