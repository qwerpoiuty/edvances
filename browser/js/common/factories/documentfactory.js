app.factory('documentFactory', function($http, AuthService) {
    var docFac = {}

    //get a single document

    //get a list of documents

    //set a single document

    //set a list of doucments

    return docFact
})